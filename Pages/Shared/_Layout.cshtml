<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="utf-8" />
    <title>@ViewData["Title"] - ÉléganceShop</title>

    <!-- Bootstrap CSS -->
    <link rel="stylesheet"
          href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" />

    <!-- Icônes Bootstrap -->
    <link rel="stylesheet"
          href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" />

    <!-- Styles -->
    <link rel="stylesheet" href="~/css/style.css" />
</head>
<body>
    <header class="site-header">
        <div class="container">
            <!-- NAVBAR -->
            <nav class="navbar navbar-expand-lg navbar-dark bg-dark-blue shadow-sm w-100">
                <div class="container-fluid">
                    <a class="navbar-brand fw-bold" href="@Url.Page("/Index")">
                        Élégance<span class="text-gold">Shop</span>
                    </a>

                    <button class="navbar-toggler" type="button"
                            data-bs-toggle="collapse"
                            data-bs-target="#mainNavbar"
                            aria-controls="mainNavbar"
                            aria-expanded="false"
                            aria-label="Menu">
                        <span class="navbar-toggler-icon"></span>
                    </button>

                    <div class="collapse navbar-collapse" id="mainNavbar">
                        <ul class="navbar-nav ms-auto mb-2 mb-lg-0">

                            @* Bonjour, Nom *@
                            @if (User.Identity?.IsAuthenticated ?? false)
                            {
                                <li class="nav-item d-flex align-items-center nav-hello">
                                    <span class="navbar-text me-3 nav-link-inline nav-hello-text">
                                        <i class="bi bi-person-circle me-1"></i>
                                        <span>Bonjour, @User.Identity.Name</span>
                                    </span>
                                </li>
                            }

                            @* Accueil: toujours visible *@
                            <li class="nav-item">
                                <a class="nav-link nav-link-inline" href="@Url.Page("/Index")">
                                    <i class="bi bi-house-door me-1"></i>
                                    <span>Accueil</span>
                                </a>
                            </li>

                            @* Vendeur UNIQUEMENT *@
                            @if (User.IsInRole("Vendeur"))
                            {
                                <li class="nav-item">
                                    <a class="nav-link nav-link-inline" href="@Url.Page("/Sales/Index")">
                                        <i class="bi bi-receipt-cutoff me-1"></i>
                                        <span>Commandes</span>
                                    </a>
                                </li>
                            }

                            @* Client UNIQUEMENT *@
                            @if (User.IsInRole("Client"))
                            {
                                <li class="nav-item">
                                    <a class="nav-link nav-link-inline" href="@Url.Page("/Cart/Index")">
                                        <i class="bi bi-cart me-1"></i>
                                        <span>Panier</span>
                                    </a>
                                </li>
                                <li class="nav-item">
                                    <a class="nav-link nav-link-inline" href="@Url.Page("/Orders/Index")">
                                        <i class="bi bi-bag-check me-1"></i>
                                        <span>Mes commandes</span>
                                    </a>
                                </li>
                            }

                            @* Liens visibles pour tout utilisateur authentifié (peu importe le rôle) *@
                            @if (User.Identity?.IsAuthenticated ?? false)
                            {
                                <li class="nav-item">
                                    <a class="nav-link nav-link-inline" href="@Url.Page("/Account/Profile")">
                                        <i class="bi bi-person-gear me-1"></i>
                                        <span>Profil</span>
                                    </a>
                                </li>
                                <li class="nav-item">
                                    <form method="post" asp-page="/Account/Logout" style="display: inline;">
                                        <button type="submit" class="nav-link nav-link-inline" style="background: none; border: none; padding: 0; margin-left: 0.75rem; cursor: pointer;">
                                            <i class="bi bi-box-arrow-right me-1"></i>
                                            <span>Déconnexion</span>
                                        </button>
                                    </form>
                                </li>
                            }
                            else
                            {
                                @* Liens pour utilisateur non connecté *@
                                <li class="nav-item">
                                    <a class="nav-link nav-link-inline" href="@Url.Page("/Account/Login")">
                                        <i class="bi bi-box-arrow-in-right me-1"></i>
                                        <span>Connexion</span>
                                    </a>
                                </li>
                                <li class="nav-item">
                                    <a class="nav-link nav-link-inline" href="@Url.Page("/Account/Register")">
                                        <i class="bi bi-person-plus me-1"></i>
                                        <span>Inscription</span>
                                    </a>
                                </li>
                            }
                        </ul>


                        <div class="navbar-social ms-lg-3 mt-3 mt-lg-0">
                            <a href="#" class="nav-social-icon">
                                <i class="bi bi-facebook"></i>
                            </a>
                            <a href="#" class="nav-social-icon">
                                <i class="bi bi-instagram"></i>
                            </a>
                            <a href="#" class="nav-social-icon">
                                <i class="bi bi-twitter-x"></i>
                            </a>
                        </div>
                    </div>
                </div>
            </nav>
        </div>
    </header>

    <main role="main">
        <section class="hero">
            <div class="container">
                <h1>Le raffinement à portée de main</h1>
                <p>Produits élégants • Qualité premium • Livraison rapide</p>
                <a class="btn btn-primary hero-button" href="@Url.Page("/Index")">
                    Voir la boutique
                </a>
            </div>
        </section>

        <div class="container py-4">
            @RenderBody()
        </div>
    </main>

    <footer class="site-footer">
        <div class="container">
            <p>&copy; @DateTime.Now.Year - ElegenceShop. Tous droits réservés.</p>
        </div>
    </footer>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

    @RenderSection("Scripts", required: false)
</body>
</html>